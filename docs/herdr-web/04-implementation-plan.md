# 04 — Implementation Plan — Herdr Harness Web App (iOS parity)

**Author:** planner subagent (qwen3.8-27b-bf16:medium), read-only. **Date:** 2026-08-18.
**Inputs:** `docs/herdr-web/03-feasibility-assessment.md` (load-bearing: 5 decisions, 2 server changes, 9 risks, 8 open questions), `docs/herdr-web/00-handoff.md` (scope §5-§7, gotchas §11, workflow §9), targeted: `01-investigation-ios-app.md` (§4.4/§4.5/§5/§6), `02-investigation-api-server.md` (§2/§3/§7), Phase 1 `frontend/harness-web/` (package.json, vite.config.ts, src inventory).

**Orchestrator verification (2026-08-18):** in-worktree reference file `cmux_harness/server.py` exists in the worktree with `_serve_orchestrator_v2_static` at **line 262** (the plan originally cited :312, which is the main-repo copy's line — corrected below to :262); `import os` at `herdr_harness/server.py:8` ✓; launch-script anchor `export HERDR_HARNESS_TAILSCALE_HTTPS_PORT=8461` at line 7 ✓; all four Swift fixture files exist (`TerminalGridHardeningTests`, `PiConversationReducerTests`, `PiConversationDecodingTests`, `PiContextUsageTests`) in an 18-file `herdr-harness-iosTests/` suite ✓; `PiToolPresentation.swift` + `WorkspaceToolModels.swift` + `Models/DemoData.swift` exist ✓; routes `POST /api/v1/alerts/{alertId}/read` + `POST /api/v1/alerts/read-all` + `pushStatus: /api/v1/push/status` present in server ✓; current tailnet host confirmed `ronniesitym4mbp.tail1db61d.ts.net` via `tailscale serve status` (handoff §1's quoted hostname is stale — plan's call to treat doc 02 §7's live value as source of truth stands) ✓; cmux-harness checked out on `feat/harness-web-app` ✓.

**Execution model:** sequential runs of the local 27B coder (`custom-lux-27b/qwen3.8-27b-bf16`); ONE phase per run; the coder sees NOTHING else — every run prompt is self-contained (paths, endpoints, contracts, acceptance, verification, do-not-touch list, all specified below per phase); the orchestrator verifies every gate (tsc/vitest/build/curls/browser) and commits (explicit `git add <paths>`, never `git add -A`) before the next run. No pushes, ever.

**Global do-not-touch (repeat in EVERY run prompt):**
- Worktree dirty files — `cmux-herdr-harness` @ `herdr-harness-ios/herdr-harness-ios.xcodeproj/project.pbxproj`, `.../xcshareddata/xcschemes/herdr-harness-ios.xcscheme`, `herdr_harness/client.py`, `herdr_harness/service.py` — never edit, stage, commit, or revert (verified 2026-08-18: these 4 are the exact dirty set).
- `frontend/harness-web/` (Phase 1 app) — read-only reference.
- The 9091 cmux server and its launchd job — never stop/restart (both-servers dependency is handled client-side, §Cross-cutting).

---

## Decisions assumed (veto at plan review)

Ronnie approved proceeding without answering doc 03's 8 open questions; the plan proceeds on all RECOMMENDED options plus plan-local calls. Rows marked ⚖️ need explicit sign-off at review, not just assumption.

| # | Decision | Assumed value | Doc 03 ref |
|---|---|---|---|
| 1 | Serve path | Same-origin, pre-auth, from 9092 at `/herdr-web` (Vite `base:"/herdr-web/"`); `HERDR_HARNESS_CORS_ORIGIN` left env-only as a `vite dev` escape hatch, never in the launch script | OQ1 / Decision 2 |
| 2 | HerdPulse | No Live-Activity UI in v1 (no browser equivalent; no GET endpoint exists; counts already in `/workspaces` state). Web-Notifications "pulse" = cheap follow-up, not v1 | OQ2 |
| 3 | Web Push / APNs replacement | Later — no VAPID endpoints exist; APNs inert on this Mac (`apns.configured:false`) | OQ3 |
| 4 | Pi Chat depth | Full semantic conversation in v1 (banner, context meter, timeline, thinking disclosure, tool cards, interaction cards, status bar, model/thinking chips) | OQ4 / Decision 5 |
| 5 | Demo fixtures | QA against the real `default` session (51 Pi panes, 50 bridge-connected, 3 capability variants — the skew the UI must survive); port `DemoData` as a mock fetch adapter for offline dev/CI + vitest | OQ5 |
| 6 | ⚖️ Voice | **Out of v1** (server contract is strictly 16 kHz mono WAV; browser capture is webm/opus — a resample pipeline is a project, not a toggle). Mic chip `voice` renders **disabled, no action** (layout parity). | OQ6 / Decision 5 voice row |
| 7 | Haptics + dead code | Skip haptics; exclude `FleetSummaryView`, `PaneSessionHeader` (verified 0 references) | OQ7 |
| 8 | Deep link + layout | `#pane=<id>` hash route + pending-pane-until-data queue (Phase-1 `hashRoute.ts` pattern); sidebar drawer → persistent left rail at desktop widths | OQ8 |
| 9 | Worktree build artifacts | `vite build` output lands in worktree `herdr_harness/static/herdr-web/` and is left **untracked** (generated at deploy; the worktree gets exactly one committed file: `herdr_harness/server.py`). Diverges from Phase 1 (same-repo assets, 431 tracked files) because the server repo is Ronnie's dirty worktree. Veto → instead commit the assets dir scoped (`git add herdr_harness/static/herdr-web` only) | Plan-local |
| 10 | Phase reorder vs doc 03 skeleton | Server phase (mount + launch-script fix + restart) is **P1, standalone**, before UI phases — the restart is Ronnie's approval gate and un-503s terminal QA; skeleton's P0 is split into P0 (scaffold, frontend-only) + P1 (server) | Plan-local |
| 11 | Pi split | Pi = 3 phases (reducer/models → per-pane store → views/composer) instead of one; the 761-line reducer port is a single flagged risk run, fixtures-first | Plan-local |

---

## Repository & deploy layout

**Two repos, strict split:**

| Repo / path | Role | Committed here |
|---|---|---|
| `cmux-harness` @ `frontend/herdr-web/` (new dir, next to `harness-web/`) | ALL frontend code | every frontend file + its tests |
| `cmux-herdr-harness` worktree (branch `codex/herdr-harness` @ `78a6548`) | 9092 server | **exactly one file:** `herdr_harness/server.py` (static mount) |
| `~/.config/herdr-harness/launch-herdr-harness.sh` (outside git) | launchd env | 1 added line, verified live, never committed |
| worktree `herdr_harness/static/herdr-web/` | **generated** build output (untracked, decision #9) | — |

**Vite config** (`frontend/herdr-web/vite.config.ts`, mirroring verified Phase-1 values): `base: "/herdr-web/"`, `build.outDir: "../../cmux-herdr-harness/herdr_harness/static/herdr-web"`, `emptyOutDir: true`. Resolves to `/Users/ronnierocha/Documents/Development/cmux-herdr-harness/herdr_harness/static/herdr-web`. **Verified sensible:** the dir is inside the worktree the running server executes from (`launch-herdr-harness.sh` does `cd` worktree → `exec … herdr_dashboard.py`), the server resolves it relative to its own file (`os.path.dirname(__file__)/static/herdr-web`), and the dir does not exist yet (created by the first build). Hash routing means the only deep path served is `/herdr-web/` → `index.html`; no SPA fallback route needed.

**Branch assumption (cmux-harness):** continue on `feat/harness-web-app` (Phase 1 branch, unpushed, already carries the reusable frontend; orchestrator-verified checked-out 2026-08-18). If Ronnie wants a sibling branch, create it — nothing in this plan depends on the name.

**Token/auth flow (byte-exact from code, doc 02 §1/§7):**
- Token file: `~/.config/herdr-harness/api-token`. Browser: onboarding modal (`Pairing token`) or `#token=<t>` fragment → localStorage key `herdr.web.token` (new key; never reuse Phase 1's). Fragment is stripped by the browser before any request; the token then lives only in localStorage.
- Every request (REST + all three SSE streams) carries `Authorization: Bearer <token>` — header-only server check (`hmac.compare_digest`, `server.py:290-310`), **no loopback bypass** (verified: a browser at `http://localhost:9092` needs the token, same as tailnet). 401 body: `{"ok":false,"error":{"code":"unauthorized","message":"A valid bearer token is required"}}` → app returns to onboarding.
- Base URL default `http://127.0.0.1:9092/api/v1`; onboarding saves a server URL (default suggestion from `GET /api/v1/network` → `urls.tailscaleSuggested`).

**What the running service serves after deploy (post-P1):** 9092 keeps everything as-is; new pre-auth `GET /herdr-web` → `index.html`, `GET /herdr-web/assets/*` → built JS/CSS with content types. Tailnet: `https://ronniesitym4mbp.tail1db61d.ts.net:8461/herdr-web/#token=<token>` (hostname orchestrator-confirmed via `tailscale serve status` 2026-08-18; the handoff's quoted hostname is stale — re-confirm at P13 anyway). `GET /` (setup page) and all `/api/v1` routes unchanged; `/panes/{id}/stream` goes from 503 → 200 after the launch-script fix + restart.

---

## Phase table

File counts are approx (impl + tests). "Gate" = orchestrator runs, all must pass before commit.

| # | Phase | Repo | Files (approx) | What | Verification gate | Est. runs |
|---|---|---|---|---|---|---|
| P0 | Scaffold + Bearer client + `sse.ts` | cmux-harness | ~10 (7 trivial ports) | New app shell, Vite config pointed at worktree static dir, Phase-1 `client.ts` ported to Bearer, new fetch-SSE client (3 cursor kinds) | tsc, vitest, build; live `curl` health sanity | 1 |
| P1 | 🚩 Server: static mount + launch script + **restart milestone (Ronnie approves)** | worktree + `~/.config` | 2 files (+1 line) | `/herdr-web` pre-auth mount in `_dispatch`; `HERDR_BIN_PATH` export; kickstart `gui/501/com.ronnierocha.herdr-harness`; first `vite build` deploy | `curl` matrix: `/` 200, `/herdr-web/` 200 (post-build), API no-token 401, `/stream` 200 (was 503); scoped commit of `server.py` only | 1 + milestone |
| P2 | Global SSE + connection/workspaces/alerts stores | cmux-harness | 5-7 | `eventStream` (ready/stream.reset resync, debounce, pi.\* journal ignore), `connectionStore` rework, `workspacesStore` (selectors, ranking, repair), `alertsStore` | tsc, vitest (synthetic gap fixture), build | 1-2 |
| P3 | Shell UI: sidebar + workspace list + Attention Deck | cmux-harness | 8-12 | 3-region layout, hash routing, sidebar rail, workspace cards + filter + AttentionStrip, Attention Deck (2 sections, NEW capsule, mark-read) | tsc, vitest, build; browser at 9092 (live data) | 2 |
| P4 | TerminalGrid port (`grid.ts`) | cmux-harness | 3-4 | 419-line Swift grid → TS (frames, ANSI subset, palette, wrap/scroll, trim) + ported Swift tests | tsc, vitest (1:1 ported cases), build | 1-2 |
| P5 | Terminal store + refresh policy + **live delta capture** + TerminalView | cmux-harness | 5-6 | `refreshPolicy` (850 ms/25 s rules), `terminalStore` (stream + poll + arbitration), view (`connecting/live/watching/offline`, `W×H · fN`); capture 2-3 real delta records → fixtures | tsc, vitest (incl. captured deltas), build; **live**: frames render, keypress updates grid, reconnect works | 1-2 |
| P6 | Pi models + reducer port | cmux-harness | 5-7 | 28 `Pi*` types, 761-line `PiConversationReducer` port, envelope/SSE-parser port; fixtures = full Swift test tables | tsc, vitest (all ported reducer/decoding/context cases), build | 1-2 ⚠️ flagged |
| P7 | Pi per-pane store | cmux-harness | 3-4 | `PiConversationStore` port: follow(), legacy 2 s poll + auto-upgrade, SSE w/ `Last-Event-ID`, reset→snapshot reload | tsc, vitest (mocked streams), build; live `curl pi/events` shape check | 1 |
| P8 | Pi Chat views + composer | cmux-harness | 10-14 | Banner/context meter/timeline/thinking/tool cards/interaction cards; composer + status bar + model/thinking chips; per-pane capability gating (3 variants) | tsc, vitest (3-variant capability fixtures), build; **live**: streaming Pi pane renders, prompt sent, 501 paths graceful | 2-3 |
| P9 | Command Lens + key deck + attachments | cmux-harness | 7-10 | Status-aware composer (`run` vs `prompt` vs Pi), 8-key deck → `send-keys`, aux bar (`attach`/`voice` disabled/`@ file`/`jira`), file-search + Jira modals, attachments base64 pipeline | tsc, vitest, build; **live**: command runs in pane, prompt reaches agent, attachment attached | 2 |
| P10 | Topology radar + pane/workspace mutations | cmux-harness | 6-9 | SVG radar from `layouts[].panes[].rect`, hero, pane list/PaneCard, menus + confirm modals, `mutations.ts` (all POST/PATCH/DELETE), post-mutation refresh + toasts | tsc, vitest (rect math vs live sample), build; **live**: split/focus/close via menu, radar + selection update via SSE | 1-2 |
| P11 | Git / Skills / Files / Jira re-point + error cards | cmux-harness | 6-10 | Phase-1 Git/Skills components re-pointed at `/api/v1/workspaces/{id}/git*`/`skills`; byte-exact strings; generic per-tool 502 error card | tsc, vitest (mocked 502 card), build; **live**: git diff + stage/unstage, skills 3 insert styles | 2 |
| P12 | Settings + onboarding + demo adapter + responsive 3-column + deep links | cmux-harness | 6-9 | Settings form (push row from `/push/status`), onboarding strings, `DemoData` mock adapter, 3-column/iPad/phone breakpoints, `#pane=<id>` pending queue, route repair | tsc, vitest, build; browser at 3 viewports + direct deep link | 1-2 |
| P13 | Polish + adversarial review + browser QA | both | fix-list only | Full test pass, reviewer subagent on full diff, fix runs, CDP screenshot QA, tailnet matrix, dirty-file integrity check | All gates green; 7 DoD items checkable live; worktree `git status --short` = original 4 lines | 1-2 + QA |

Total ≈ 17-24 coder runs. Doc 03's 8-phase skeleton is refined: its P0 split into P0+P1 (restart gate isolated), P1 split into P2+P3 (stores vs views), P2 into P4+P5 (grid port vs store/view/capture — grid is the highest-risk port and must be testable offline), P3 into P6+P7+P8 (reducer → store → views; the 761-line reducer stays one run but fixtures-first, risk stated), P6 split into P9+P10+P11+P12 (command lens is not terminal-adjacent enough to share a run with the radar; tool re-pointing needs error-card infra first).

---

## Phase detail

Every run prompt (orchestrator template, per phase): ① one-sentence goal; ② "you are implementing phase Pn of the herdr-web app; you have no other context — this prompt is complete"; ③ exact absolute paths to create/modify; ④ reference files to READ first (absolute paths, incl. worktree Swift + this doc's §6/§3 as needed); ⑤ the contracts below (endpoints, wire shapes, byte-exact strings — or the instruction "read `docs/herdr-web/01-investigation-ios-app.md` §6 and match those strings byte-for-byte"); ⑥ do-not-touch list (global, above + phase-specific); ⑦ "do not commit; do not run git; do not add dependencies; stop when the file list is done and report what you built"; ⑧ verification commands the orchestrator will run (so the coder can self-check).

### P0 — Scaffold + Bearer client + `sse.ts` (cmux-harness)

**Goal:** a compiling, testable `frontend/herdr-web` app with working Bearer transport and the shared fetch-SSE client; no UI beyond a token gate.
**Create:** `frontend/herdr-web/{package.json, vite.config.ts, tsconfig.json, index.html, src/main.tsx, src/App.tsx, src/styles/global.css, src/api/client.ts, src/api/sse.ts, src/api/client.test.ts, src/api/sse.test.ts}`.
**Run prompt must include:**
- Port from `frontend/harness-web/` (read those files): `package.json` (rename `herdr-harness-web`; same dep set — do NOT add deps), `tsconfig.json`, `global.css`, `App.tsx` pattern (token gate → probe → placeholder body).
- `vite.config.ts`: `base:"/herdr-web/"`, `outDir:"../../cmux-herdr-harness/herdr_harness/static/herdr-web"`, `emptyOutDir:true`.
- `client.ts` deltas vs Phase 1: header `Authorization: Bearer` (replace `X-Cmux-Token`), token key `herdr.web.token`, default base `http://127.0.0.1:9092/api/v1`, 15 s default timeout, same abort-merge + envelope handling (`{ok:false,error:{code,message}}`), 401 → emit "unauthorized" event (onboarding re-entry).
- `sse.ts` contract (the novel core, ~250 LOC): `openSSE({url, token, cursorKind, onEvent(event,data,id), onReady(payload), onReset(payload), onState(state)})` using `fetch(url,{headers:{Authorization}})` + `response.body.getReader()`; buffer until `\n\n`; parse `id:`, `event:`, `data:` (multi-line join `\n`), ignore `: comment` heartbeats; cursor models: `"int-header"` (global — send `Last-Event-ID: <lastId>` on reconnect), `"none"` (terminal — no replay; reconnect = fresh attach, first frame is `full:true`), `"opaque-after"` (Pi — reconnect with `?after=<lastId>` AND set `Last-Event-ID`); backoff params per call site (iOS values: global 2 s ×2 cap 15 s, terminal 0.65 s ×1.7 cap 5 s, Pi 0.65 s ×1.7 cap 6 s); `visibilitychange` → close on hidden, reopen (with cursor) on visible; honor `retry:` preamble. Tests: fake `ReadableStream` feeding synthetic SSE bytes (id/event/data/comments), reconnect-after-drop resends correct cursor for each of the 3 kinds, comment lines dropped, multi-line data joined.
**Verify (orchestrator):** `cd frontend/herdr-web && npx tsc --noEmit && npm test && npm run build` (build writes into the worktree static dir — expected, untracked); live sanity: `curl -s -H "Authorization: Bearer $(cat ~/.config/herdr-harness/api-token)" http://127.0.0.1:9092/api/v1/health` → `{"ok":true,...herdr.connected:true...}` (API reachable; app not yet served — `/herdr-web/` still 404s, that's correct pre-P1).
**Done:** gates green; assets exist at `cmux-herdr-harness/herdr_harness/static/herdr-web/`; commit in cmux-harness (branch per §layout).

### P1 — 🚩 Server: static mount + launch script + restart (worktree)

**Goal:** 9092 serves `/herdr-web` pre-auth and the terminal stream stops 503ing. The ONLY phase that touches the running launchd service.
**Modify (exactly 2 files):**
1. `cmux-herdr-harness/herdr_harness/server.py` — module-level `_HERDR_WEB_STATIC = os.path.join(os.path.dirname(__file__), "static", "herdr-web")` (`os` imported at line 8, verified) + handler method `_serve_herdr_web_static(self, path)` modeled on the in-worktree reference `cmux_harness/server.py:262` `_serve_orchestrator_v2_static` (orchestrator-verified present in the worktree at line 262 — coder reads it): `/herdr-web` (bare; `_parse` rstrips trailing `/`) → `static/herdr-web/index.html`; `/herdr-web/<rel>` → resolve + `os.path.commonpath` traversal guard; content-type map (html/js/css/svg/png/json); `read_bytes` + 200 + Content-Length; return `True/False` so dispatch falls through when absent/404. One dispatch branch inserted **after** the `path == "/"` block and **before** the `if len(segments) < 2 or segments[:2] != ["api","v1"]` 404 check (anchor `herdr_harness/server.py:340-348`, verified): `if method == "GET" and self._serve_herdr_web_static(path): return`.
2. `~/.config/herdr-harness/launch-herdr-harness.sh` — insert `export HERDR_BIN_PATH=/opt/homebrew/bin/herdr` after the `HERDR_HARNESS_TAILSCALE_HTTPS_PORT=8461` line (orchestrator-verified anchor: line 7; line absent today).
**🚩 Milestone M1 — STOP and get Ronnie's explicit OK before running:** `launchctl kickstart -k gui/501/com.ronnierocha.herdr-harness` (job verified present via `launchctl list`, PID 63510 pre-restart). Gotchas (doc 00 §11): KeepAlive revives killed PIDs — kickstart is the only supported restart; the 9091 wrapper's `pgrep` is too broad — never use it.
**Deploy + verify (orchestrator, post-restart):**
1. `cd frontend/herdr-web && npm run build` → assets in worktree static dir (untracked).
2. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:9092/` → **200** (setup page intact).
3. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:9092/herdr-web/` → **200** text/html (`<!doctype html`); `curl -s http://127.0.0.1:9092/herdr-web/../server.py | head -c 40` → traversal guard holds (404/fall-through); no token required for static (pre-auth, same as `/`).
4. `curl -s http://127.0.0.1:9092/api/v1/health` (no token) → **401** byte-exact body; with token → 200 `herdr.connected:true`.
5. **Terminal un-503 check (the point of the fix):** pick any live pane id (e.g. `w1:p1` from `/api/v1/workspaces`), `curl -s -N --max-time 8 -H "Authorization: Bearer $(cat ~/.config/herdr-harness/api-token)" "http://127.0.0.1:9092/api/v1/panes/w1:p1/stream?cols=100&rows=32"` → `event: ready` then a `terminal.frame` record (NOT the old 503 `terminal_observer_unavailable`).
6. Tailnet: `curl -sk -o /dev/null -w "%{http_code}" https://ronniesitym4mbp.tail1db61d.ts.net:8461/herdr-web/` → 200 (host from `tailscale serve status`).
7. Commit in worktree: `git add herdr_harness/server.py && git commit -m "[HERDR] Serve /herdr-web static mount pre-auth"` — `git status --short` afterwards must show exactly the original 4 dirty lines and `?? herdr_harness/static/` only.
**Done:** all six checks green; launchd job restarted once; commit scoped to one file.

### P2 — Global SSE + connection/workspaces/alerts stores (cmux-harness)

**Goal:** the event-driven state core — no polling loop; `stream.reset` is a silent resync, never an error (doc 03 risk 1).
**Create:** `src/store/eventStream.ts`, `src/store/connectionStore.ts` (rework of Phase 1's, read it first), `src/store/workspacesStore.ts` (rework), `src/store/alertsStore.ts`, `src/api/herdr.ts` (wrappers: `health`, `workspaces`, `workspace(id)`, `alerts({unread,limit,status})`, `alertRead(id)`, `alertsReadAll`), + tests. Save live-shape fixtures as `src/__fixtures__/workspaces.json`, `alerts.json`, `events-sample.json` (orchestrator pre-captures with the token; shapes: doc 02 §7 — workspace composite keys `workspace_id,number,label,focused,pane_count,tab_count,active_tab_id,agent_status,tabs[],panes[],agents[],layouts[]`; alert sample; `ready` + `pi.message_update` SSE samples).
**Run prompt must include (dispatch table — copy verbatim into the prompt):**
- `/api/v1/events` via `sse.ts` `cursorKind:"int-header"`. `ready` → store `lastEventId`, mark connected. `stream.reset` (`reason:"replay_gap"|"backend_restarted"`) → **silent** `/workspaces` refetch, no user-visible error. `snapshot.updated` → debounced (~500 ms) `/workspaces` refetch. `connection.changed` → connectionStore. `alert.created`/`alert.updated` → alertsStore upsert + unread badge. `pi.bridge.connection`, `pi.session_start`, `pi.session_shutdown`, `pi.session_info_changed`, `pi.session_tree` → silent debounced refetch. `push.*` → ignore. **Every other event (raw herdr events — payload nested at `data.data` — and all `pi.*` journal re-publications, ~1366 in 14 s live) → same debounced refetch arm**; never parse individual journal events into global state (doc 03 risk 2 mitigation).
- `connectionStore`: initial `/health` probe (`herdr.connected`, `cache.stale`, `alerts.unread` — live shape doc 02 §7) + `connection.changed` events + stream liveness; states `Offline`/`Connecting`/`Live`/`Demo`/`Unavailable` (byte-exact, doc 01 §6).
- `workspacesStore` selectors (iOS parity, doc 01 §2): `visibleWorkspaces`, `attentionPanes` ranked blocked→done→working→idle→unknown by `attentionRank` then `revision` desc, `unreadAlertCount`, `canControl`; post-mutation silent refresh hook; `repairSelection()` (prune dead workspace/pane selections, reselect first workspace — route-guard input for P3/P12).
**Tests (required):** `eventStream` — synthetic stream: `ready` → replay batch → `stream.reset replay_gap` → exactly one silent refetch; 3 `snapshot.updated` within 500 ms → 1 refetch; 100 `pi.message_update` → 0 refetches, 0 errors surfaced. `workspacesStore` — ranking against the fixture (5 statuses + revision tie-break).
**Verify:** tsc/vitest/build. Live: `curl` `/workspaces` and diff the fixture keys against the store's parsed type (tsc enforces; orchestrator eyeballs one live alert object through the store in devtools optional).
**Done:** gates green; commit.

### P3 — Shell UI: sidebar + workspace list + Attention Deck (cmux-harness)

**Goal:** the navigable app shell with byte-exact strings; live data flowing from P2.
**Create:** `src/App.tsx` (3-region layout: left rail | list | detail placeholder; wire `eventStream` at mount; 401 → onboarding), `src/lib/hashRoute.ts` + test (port Phase 1; extend with `#ws=<id>&pane=<id>`), `src/hooks/useOverlay.ts` (port), `src/lib/workspaceGroups.ts` + test (port, herdr field names), `src/components/Sidebar.tsx` (persistent rail ≥ desktop width; overlay drawer narrow; `chats`, `filter chats`, `new workspace`, `N total shown`, `no panes yet`, `Close navigator`; collapse set → localStorage), `src/components/Workspace/WorkspaceListView.tsx` + `WorkspaceCardView.tsx` + `AttentionStrip.tsx` (search; segmented `All`/`Needs you`/`Active` (lowercased render); `Workspaces` title; `choose a workspace`; `herdr`/`switch`; `Demo data is active`; `spaces` + `n / total`; `new workspace`; top-2 AttentionStrip with `open queue`; `active` badge; branch fallback `shell`; empty: `No Herdr workspaces` + `Create a workspace here or on your Mac to begin.`; placeholders `Choose a workspace`/`Its tabs and panes will appear here.`/`Choose a pane`/`Open a terminal or agent session.`), `src/components/Attention/AttentionView.tsx` + `AlertCardView.tsx` (title `Attention`; deck header `Attention deck`; subcopy `Blocked first, then unseen completions. The queue stays quiet until there's a decision worth making.`; `Recent signals` (journal) + `Live queue` (ranked) sections; empty `Nothing needs you` + `Working agents will surface here when they finish or need a decision.`; `NEW` capsule; `Closed pane · <paneID>`; mark-read `POST /alerts/{id}/read`; read-all via `POST /alerts/read-all`; **assumes 123/125 unread on day one — never clear-on-load** (doc 03 risk 8); alert card action `open_pane` → deep link).
**Run prompt must include:** "read `docs/herdr-web/01-investigation-ios-app.md` §6 (byte-exact strings) and `docs/herdr-web/02-investigation-api-server.md` §7 (shapes); every literal user-facing string must match §6 byte-for-byte, including lowercase renders and the ellipsis char `…`". Status vocabulary: titles `Needs you`/`Ready`/`Working`/`Idle`/`Shell`, compact `Blocked`/`Done`/`Working`/`Idle`/`Unknown` (often lowercased).
**Runs:** 2 — (a) layout + sidebar + workspace list; (b) Attention Deck.
**Verify:** tsc/vitest/build; **browser** (orchestrator: build → `http://127.0.0.1:9092/herdr-web/#token=…`, throwaway Chrome + CDP screenshots): list renders 15 live workspaces with correct status colors/ranking; deck shows live queue + journal; mark-read decrements badge; deep-link `#ws=w1` selects.
**Done:** gates + visual pass; commit.

### P4 — TerminalGrid port (cmux-harness) ⚠️ highest-risk port

**Goal:** a deterministic TS port of `TerminalGrid.swift` (419 lines) with the Swift test suite as acceptance fixtures. No network, no view.
**Create:** `src/terminal/grid.ts` (~600-800 LOC) + `src/terminal/grid.test.ts` + `src/terminal/palette.ts` (if split) + fixtures.
**Run prompt must include (contracts):**
- Read first (absolute paths): `cmux-herdr-harness/herdr-harness-ios/herdr-harness-ios/Models/TerminalGrid.swift` (the source of truth), `cmux-herdr-harness/herdr-harness-ios/herdr-harness-iosTests/TerminalGridHardeningTests.swift` (**fixture file — port every test case 1:1 into vitest**), and Phase 1 `frontend/harness-web/src/terminal/ansi.ts` (reuse its SGR→CSS color resolution; extend, don't fork semantics).
- API: `class TerminalGrid { apply(frame: TerminalFrame): void; resize(w,h): void; get visibleRows(): Row[]; cursor; lastSequence }`; frame = `{type:"terminal.frame", bytes:base64, encoding:"ansi"(ignore value), full, height, seq, width}`; decode bytes base64→UTF-8.
- `full:true` → resize grid to frame dims + full reset + parse. `full:false` → **reject if `seq <= lastSequence`** (stale deltas silently dropped) → resize (copy overlap, clamp cursor) → parse. (Contract from `TerminalGrid.swift:106-121`; deltas unverified on the wire — P5 captures them, doc 03 risk 3.)
- Parser semantics (copy from Swift): ESC CSI subset `H f A B C D G d J K m` + private `?25h/l` (cursor visibility); OSC sequences consumed and ignored; `\r \n \b \t` with 8-col tabs; pending auto-wrap at last column; scroll-on-linefeed at bottom; SGR reset/bold/italic/underline/inverse + 30-37/39/40-47/49/90-97/100-107 + **38/48 with `2;r;g;b` and `5;n`**; palette = **copy the 16 hardcoded base hex values verbatim from `TerminalGrid.swift`** (Herdr/Catppuccin-aligned) + 216 cube + 24-step ramp; default 100×32; `visibleRows` trims trailing blanks (cursor extends).
- Render model: rows of styled cell runs → the view (P5) maps to a 32-row `<pre>`/grid of spans; inverse-rendered cursor.
**Risk statement (in prompt + here):** this is the largest single port after the Pi reducer; mitigation = fixtures-first (ported Swift tests written before/alongside impl; grid must pass them all). If the 27B run produces a half-port, orchestrator splits it: run A = state machine (apply/resize/parse), run B = palette + rendering + remaining tests.
**Verify:** tsc; vitest — **every ported `TerminalGridHardeningTests` case green** (orchestrator counts: Swift cases = vitest cases); build.
**Done:** gates green; commit.

### P5 — Terminal store + refresh policy + live delta capture + TerminalView (cmux-harness)

**Goal:** a live pane renders streaming frames with snapshot arbitration — terminal parity.
**Create:** `src/terminal/refreshPolicy.ts` (port of 26-line `Models/TerminalRefreshPolicy.swift` — read it; rule: snapshot replaces grid only when (forced) OR (grid didn't advance during the request) OR (snapshot text changed without frames) OR (stream silent ≥ **25 s**; `streamSilenceLimit = 25`); failed snapshot marks `.disconnected` + error pill **only when the stream is already stale**), `src/store/terminalStore.ts`, `src/components/Terminal/TerminalView.tsx`, `src/api/terminal.ts` (`stream(paneId,cols,rows)` via sse `cursorKind:"none"`; `output(paneId)` = `GET /panes/{id}/output?source=recent_unwrapped&lines=160`), tests, `src/__fixtures__/terminal-deltas.json` (from the capture step below).
**Run prompt must include:** store contract — one pane at a time (server semaphore 8); on open: source `connecting` → initial forced snapshot → open stream `?cols=100&rows=32` (`ready` → `{paneId,cols,rows}`; each `terminal.frame` → `grid.apply` → `frameSequence=seq` → source `live`) + 850 ms snapshot poll arbitrated by `refreshPolicy`; stream failure → source `snapshot`/`offline` + backoff 0.65 s ×1.7 cap 5 s; `terminal.closed`/`terminal.error` → terminate stream, source `offline` + message; 503 `terminal_observer_unavailable` → `offline` + the server message. Byte-exact UI: labels `connecting`/`live`/`watching`/`offline` (lowercase, with colored dots), `Connecting to terminal…`, `No terminal output yet.`, toolbar `W×H · fN` (watching shows `rN` revision), selectable text.
**📸 Live delta capture (orchestrator-led, inside this phase, after build):** pick a live shell pane (e.g. `w1:p1`); drive it: `herdr terminal session send-keys <pane> 'ls' 'enter'` (or `POST /panes/{id}/send-keys` via curl); simultaneously capture: `herdr terminal session observe <pane> --cols 100 --rows 32` (NDJSON to a file); extract 1 `full:true` + 2-3 `full:false` records → save as `src/__fixtures__/terminal-deltas.json` (raw capture kept alongside for provenance); add a vitest case applying the captured full→delta sequence and asserting no corruption + expected visible rows. **If a captured delta deviates from the `TerminalGrid.swift` contract: do NOT improvise — report to orchestrator; the 850 ms snapshot arbitration bounds blast radius and snapshot-only is the accepted degraded mode (iOS has the same fallback).**
**Verify:** tsc/vitest (incl. captured-delta case + refreshPolicy table: forced / no-advance / text-changed / 25 s-silent / failed-snapshot-fresh-stream=no-pill)/build; **live:** open a pane in the browser → `live` label + frames; send a key (curl `send-keys` or later Command Lens) → grid updates; background the tab 10 s → foreground → stream resumes (full frame); `curl -N …/stream` still 200.
**Done:** delta fixtures in repo; live terminal working; commit.

### P6 — Pi models + reducer port (cmux-harness) ⚠️ flagged risk run

**Goal:** the semantic projection of a Pi session in TS — bounded, self-contained, fully fixture-tested before any view exists.
**Create:** `src/pi/types.ts` (the 28 `Pi*` model types → TS interfaces), `src/pi/envelope.ts` (wire envelope decode + `pi.`-prefix normalization + SSE parse rules from `Infrastructure/PiConversationSSEParser.swift`, 82 lines — read it: `id:` line as cursor fallback, `ready`/`pi.ready`/`pi.heartbeat` → activity, `pi.error`/`pi.stream.closed` → stream end, dispatch when accumulated `data:` forms complete JSON), `src/pi/reducer.ts` (port of `State/PiConversationReducer.swift`, 761 lines), tests.
**Run prompt must include:**
- Read first: the three Swift files above (absolute worktree paths) + `cmux-herdr-harness/herdr-harness-ios/herdr-harness-iosTests/PiConversationReducerTests.swift` (**fixture — port the entire test table 1:1**, it is the acceptance spec) + `PiConversationDecodingTests.swift` + `PiContextUsageTests.swift` + doc 02 §3.3 (event wire format: `id: <cursor>` / `event: pi.<type>` / `data: {id,event,data:{protocol,pane_id,session_id,cursor,event:{…}},generatedAt}`; 24 bridge types + 3 synthesized: `bridge.connection`, `stream.reset`, `bridge.payload_omitted`).
- Reducer semantics to replicate (doc 01 §4.4, exact): cursor de-dup with 2048-entry LRU (`remember`); `ready` and `stream.reset` bypass dedup; `stream.reset` → `needsSnapshot`; `session_tree`/`session_compact` → `needsSnapshot`; `session_start`/`session_switch` → `needsSnapshot` only on session-ID change. Phase: `agent_start`/`turn_start` → working; `agent_settled` → idle + settle all turns; `message_end`/`tool_execution_end` with `isError` or error/aborted `stopReason` → `.failed`. Turns: user message starts turn `turn:<messageID>`; assistant parts keyed `<messageID>:text|thinking:<contentIndex>` (deltas append; `text_end`/`thinking_end` finalize); tools keyed by `callID` across `toolcall_start` (placeholder `Preparing tool`) → `toolcall_end` → `tool_execution_*` → `toolResult` (succeeded/failed). Notices (in-transcript, byte-exact): `Context compacted`, `Branch context summarized`, `Model changed`, custom messages, `Pi reported an error`, `Response stopped`, `Pi could not finish` + detail fallbacks `The response was interrupted.`/`No error details were provided.`. `pendingInteractions` upsert from `extension_ui_request`/`interaction_request` (kinds select/confirm/input/editor/unknown; title fallback `Pi needs your input`), removed on `interaction_response`/`interaction_cancelled` or successful respond. Telemetry: `contextUsage` from `state.context`/`turn_end` (nulls tolerated), `currentModel` from `state.model`/`model_select`, `thinkingLevel` from `state.thinkingLevel`/`thinking_level_select`.
- **Method: fixtures-first** — port the Swift test table to vitest before writing `reducer.ts`; implement until green.
**Risk statement:** 761-line port in one 27B run is the single most likely point of a broken phase; mitigation: fixtures-first + orchestrator may split into run A (types + envelope + decoding tests) / run B (reducer) without changing the phase boundary.
**Verify:** tsc; vitest — **every ported case from the 3 Swift test files green** (orchestrator counts parity); build; live shape check: `curl -s -H token http://127.0.0.1:9092/api/v1/panes/<livePiPane>/pi/snapshot` → keys parse into `types.ts` without tsc complaints (orchestrator eyeballs one live envelope).
**Done:** gates green; commit.

### P7 — Pi per-pane store (cmux-harness)

**Goal:** live Pi conversation state per open Pi pane, with legacy-bridge degradation.
**Create:** `src/store/piStore.ts`, `src/api/pi.ts` (`snapshot`, `events(paneId, after?)` via sse `cursorKind:"opaque-after"`, `models`, `prompt/steer/follow-up` `{text}`, `abort` `{}`, `model` **exactly** `{provider,id}`, `thinking-level` **exactly** `{level}`, `respond(interactionId, {value?|confirmed?|cancelled?})`), tests.
**Run prompt must include (store contract, from `State/PiConversationStore.swift` — read it, 389 lines):** capability gate: pane's `pi_semantic` (live shape: `{available, connected, protocol_version, session_id, cursor, oldest_cursor, capabilities:{prompt,steer,followUp,abort,listModels,setModel,setThinkingLevel,interactionResponse}}`) — chat usable iff `available && protocolVersion == 1`. `follow()`: GET `pi/snapshot` → verify protocol `herdr.pi.semantic` v1 + `available` (else `.unavailable` + `This Pi session does not expose a compatible native transcript.`) → `reducer.replace(snapshot)` → if snapshot lacks `state.context` (legacy) or `connected:false` → **2 s snapshot poll** with content-change detection, backoff ×1.7 cap 8 s, auto-upgrade to SSE when a bridge appears; else open `pi/events?after=<cursor>` (+`Last-Event-ID` header). Reconnect: backoff 0.65 s ×1.7 cap 6 s, state `.reconnecting(attempt:)`, banner `Live updates paused. Reconnecting…`. `stream.reset`/`session_tree`/`session_compact` → authoritative snapshot reload (reducer `needsSnapshot`). Commands must return `{ok|success:true}` else surface the error string.
**Tests (mocked sse + snapshot):** fresh-bridge path (snapshot → SSE from cursor); legacy path (no `state.context` → poll loop → bridge appears → upgrade, no event loss); `stream.reset` → exactly one snapshot reload; reconnect resends `?after=<last>` + header.
**Verify:** tsc/vitest/build; live: `curl -s -N --max-time 8 -H token "…/api/v1/panes/<pane>/pi/events?after=0"` → `event: pi.ready` envelope + replay (shape matches `envelope.ts`); browser devtools: store follows a live streaming pane (banner, context meter values advance).
**Done:** gates green; commit.

### P8 — Pi Chat views + composer (cmux-harness)

**Goal:** the differentiating surface — full Pi Chat UI, capability-skew-safe.
**Create:** `src/components/Pi/PiChatView.tsx` (banner states byte-exact: `Loading native transcript…` / `Pi is offline. Transcript preserved.` / `Reconnecting to Pi…` / `Native transcript unavailable`; context meter bar + `12.3k / 192k` format + % + color bands fraction <0.6 / <0.85 / else; timeline: truncation header `Older context was omitted by Pi`, turns with activity rail (dot/gradient/end-dot encodes has-tool/failed/active), user bubble right, assistant **sanitized** markdown left, thinking collapsed `Thinking` (streaming, relative timer) / `Thought process` with fallbacks `Reasoning details are unavailable for this response.`/`Pi is working through the request…`/`No reasoning text was provided.`, `Jump to latest`, `Start a conversation` + `Messages, thinking, and tool activity will appear here. The terminal remains available from the pane menu.`), `src/components/Pi/ToolCard.tsx` (statuses `QUEUED`/`RUNNING` (spinner)/`DONE`/`FAILED` + elapsed + `INPUT`/`RESULT`/`ERROR` disclosures; `Waiting for tool details…`; presentation mapped by tool name → Command/Read/Write/Edit/Search/Web per `Models/PiToolPresentation.swift` — read it), `src/components/Pi/InteractionCard.tsx` (option buttons / `No`-`Yes` / text+`Submit`/`Cancel`; respond → 501 `unsupported` → notice, never crash), `src/components/Pi/PiComposer.tsx` (status-aware: `Pi is offline` / `Message Pi` / `Steer this turn` / `Queue a follow-up`; dispositions `Send`/`Steer`/`Follow up` (short `Send`/`Steer`/`Next`); status bar `Pi is working` + `Stop`; notices `Follow-up queued`/`Stop requested`/`Model set to <name>`/`Thinking set to <level>` auto-clear 2.5 s; offline send → `Pi is offline. Reconnect before sending a message.`), `src/components/Pi/ModelChip.tsx` + `ThinkingChip.tsx` (models grouped by provider from `GET pi/models`; `Loading models…`/`No models available`/`Couldn't load models`; 501 → `Model switching isn't supported by this Pi session`; levels `Off`/`Minimal`/`Low`/`Medium`/`High`/`Extra High`/`Max`; 501 → `Thinking control isn't supported by this Pi session`), capability gating: hide model chip when `setModel:false` (and `listModels:false`), hide thinking chip when `setThinkingLevel:false` — **per-pane, never global** (3 live variants; doc 03 risk 7); pane menu: chat mode only when `supportsPiSemanticChat`, auto-selected on first open unless user chose Terminal.
**Runs:** 2-3 — (a) timeline (turns/thinking/tool cards/notices); (b) composer + chips + status bar; (c) interaction cards + capability-variant view tests (may merge into b).
**Markdown rule (non-negotiable, security):** render Pi assistant markdown with a constrained renderer that emits elements only for a fixed node allowlist — **no `dangerouslySetInnerHTML`, no raw-HTML pass-through of bridge content** (doc 03 security residual: XSS is the token's only real enemy). Orchestrator greps the diff for `dangerouslySetInnerHTML` → must be zero hits.
**Tests:** view tests driven by fixture store states for **all 3 capability variants** (newer w/ setModel, older without, `interactionResponse:false` everywhere → 501 path), plus streaming-sequence fixture (message_update deltas → finalized turn) from the P6 test tables.
**Verify:** tsc/vitest/build; **live (the real acceptance):** open a Pi pane with an active session → live streaming text + thinking collapse + tool cards render; send a prompt via composer → turn appears; on an older-variant pane: chips hidden, respond → 501 notice; `pi/abort` while working → `Stop requested` + stop.
**Done:** gates + live Pi session verified; commit.

### P9 — Command Lens + key deck + attachments (cmux-harness)

**Goal:** drive panes from the web — shell commands, agent prompts, keys, files, Jira context.
**Create:** `src/components/Pane/PromptComposerView.tsx` (status-aware placeholder byte-exact: `run or type into this shell` / `message <agent>`; dispatch: shell → `POST /panes/{id}/run {command}`; detected agent → `POST /panes/{id}/prompt {text, wait?/until?/timeoutMs?}`; Pi pane → P8 PiComposer), `src/components/Pane/TerminalKeyDeck.tsx` (8 keys byte-exact: `Up` `Down` `Tab` `Enter` `Left` `Right` `Esc` `Bkspc` → `POST /panes/{id}/send-keys {keys:[...]}` — all pass server whitelist `^[A-Za-z0-9+_-]{1,32}$`; Interrupt = menu action, not a key), `src/components/Pane/ComposerAuxBar.tsx` (chips `attach` `voice` (disabled, no action — decision #6) `@ file` `jira`), `src/components/Tools/FileSearchModal.tsx` (re-point Phase 1's to `GET /workspaces/{id}/files?q&limit=80`; `WORKSPACE FILES`/`MATCHES`/`Done`/`Retry`), `src/components/Tools/JiraModal.tsx` (re-point to `GET /jira/assigned?limit=50` + `GET /jira/issue?q`; `JIRA CONTEXT`/`EXACT LOOKUP`/`LOOKUP RESULT`/`Paste a ticket key or browse URL from any project.`/`loading assigned tickets`/`Jira unavailable`/`no assigned tickets`/`Use exact lookup for another ticket.`; insert block `Jira: <key> · <title>` / `Status: <s> · Priority: <p>` / `<url>`), `src/store/attachmentsStore.ts` (port Phase 1's: `POST /workspaces/{id}/attachments {filename,content_type?,data_base64}` ≤20 MB; states `uploading`/`attached`/`upload failed`; 90 s timeout), toast wiring (`Sent to <agent>`, `Reconnect before controlling Herdr`).
**Runs:** 2 — (a) composer + key deck + run/prompt dispatch; (b) aux bar + file/Jira modals + attachments.
**Insert behavior:** skill/file/Jira inserts force Terminal mode + refocus composer (doc 01 §4.3).
**Verify:** tsc/vitest/build; **live:** type `ls` in a shell pane via lens → executes, output appears in P5 terminal view; key deck `Up`/`Enter` drives history; `prompt` an agent pane → agent responds (visible in terminal); attach a text file → `attached` chip; `@ file` picks from search; Jira modal shows assigned IOSDOX tickets, insert block lands in composer.
**Done:** gates + live checks; commit.

### P10 — Topology radar + pane/workspace mutations (cmux-harness)

**Goal:** spatial workspace view + full mutation surface with confirm dialogs and post-action refresh.
**Create:** `src/components/Workspace/TopologyRadar.tsx` (pure SVG rect math: input `layouts[].panes[].rect` inside `area` (terminal-cell coords — live sample in doc 02 §7: `area {x:36,y:1,width:204,height:56}`, pane rects); scale area-relative, 4 pt corner radius, focused pane highlight, selected-pane outline; `zoomed` flag respected), `src/components/Workspace/WorkspaceHero.tsx`, `src/components/Workspace/PaneListView.tsx` + `PaneCard.tsx` (`tabs[]` grouping, loose panes fallback; rail + `rev N` + agent icon + `pane.id`), `src/components/Workspace/WorkspaceMenu.tsx` + `PaneMenu.tsx` (items byte-exact: `Workspace actions`, `Pane actions`, `View`, `Focus on Mac`, `Interrupt`, `Rename workspace`/`Workspace name`, `Rename pane`/`Pane name`, `New tab`, `Refresh`, `Close workspace`, `Close pane`, `Split pane`, `Split right`, `Split down`, `Start agent` → `Codex`/`Claude`/`OpenCode`, `Open workspace`; rename copy `The new label appears in Herdr on every connected client.`/`This label is shared with Herdr on your Mac.`), `src/components/Shared/ConfirmModal.tsx` (`Close this workspace?` + `All N pane processes in this workspace will stop.`; `Close this pane?` + `This stops the process running in <title>.`), `src/api/mutations.ts` (all POST/PATCH/DELETE from doc 02 §2: workspaces create `{cwd?,label?,focus?,env?}` / PATCH label / DELETE / focus / tabs; panes PATCH label (nullable) / DELETE / focus / split `{direction: right|down, focus?, cwd?, env?, ratio? 0.05-0.95}` / send-text / send-keys / run / prompt / start-agent `{name,kind,args?,timeoutMs}` / tabs PATCH/DELETE/focus), toasts (`Pane split`, `Focused on Mac`, `Pane closed`, `Pane renamed`, `Workspace focused on Mac`, `Workspace renamed`, `Workspace closed`, `Workspace created`, `Tab created`, `<Kind> started`, `Reconnect before controlling Herdr`).
**Runs:** 1-2 — (a) radar + hero + pane list + rect-math tests (fixture = the doc 02 §7 live layout sample); (b) menus + confirms + mutations + toasts.
**After every mutation:** silent `/workspaces` refresh (store hook from P2) + `repairSelection()` if the selected pane/workspace died.
**Verify:** tsc/vitest/build; **live:** radar matches the real layout (screenshot vs Mac); `Split right` on a pane → SSE `snapshot.updated` → radar + list update without manual refresh; close pane with confirm → selection repaired; `New tab` → `Tab created` toast; `Start agent` (use a safe pane) → `<Kind> started`.
**Done:** gates + live checks; commit.

### P11 — Git / Skills / Files / Jira re-point + error cards (cmux-harness)

**Goal:** the cmux-proxied tool tabs work against 9092 `/api/v1/workspaces/{id}/…` with per-tool failure surfaces (both-servers dependency, doc 03 risk 5).
**Create:** `src/components/Git/GitStatusView.tsx` + `DiffSheet.tsx` (re-point Phase 1's Git components at `GET /workspaces/{id}/git` (`branch, staged[], unstaged[], untracked[], commits[]`), `GET …/git/diff?file&section` (section default `unstaged`), `POST …/git/stage|unstage {file}`; byte-exact: `detached`, `clean`, `N changed`, `staged`/`unstaged`/`untracked`, `recent commits`, `Working tree clean` + `Everything in this workspace is committed.`, `No Git data` + `This workspace does not have a Git repository yet.`, `Reading workspace Git state…`, `Git unavailable` + `Try again`, `Stage`/`Unstage`, `Refresh Git`, `Diff unavailable`, `Loading diff…`, `Done`, `(empty diff)`; toasts `Staged <file>`/`Unstaged <file>`; 30 s timeouts), `src/components/Tools/SkillsTab.tsx` (re-point to `GET /workspaces/{id}/skills`; byte-exact: `workspace skills`, `N found`, `Refresh skills`, `Add to terminal`, `Choose a skill, then insert it as a Claude command, Codex invocation, or file reference.`, `Indexing project and user skills…`, `Skills unavailable`, `No skills found`, `Add project skills under .claude/skills or user skills under your configured skills directory.`; 3 insert styles byte-exact: `Claude Code` `/name`, `Codex CLI` `$name`, `Skill file path` `` `path` `` (tokens per `Models/WorkspaceToolModels.swift:126-146` — read it); insert → Terminal mode + refocus), `src/components/Shared/ToolErrorCard.tsx` (generic: icon + `X unavailable` + message + `Try again`/`Retry`; used by git/skills/files/jira on 502/503-upstream from the cmux proxy and on timeout), Files + Jira modals already exist from P9 — wire them to the same error card.
**Runs:** 2 — (a) Git tab + error card infra; (b) Skills + error-card wiring across tools.
**502 verification WITHOUT touching the 9091 service** (launchd-managed — never stopped for QA): error-card rendering tested in vitest with mocked 502/timeout responses; live happy-path verified against real 9091 (which is up).
**Verify:** tsc/vitest (incl. mocked 502 card cases)/build; **live:** git tab on a git workspace → correct branch/staged/untracked; open a diff; stage + unstage a file → list updates + toast; skills tab on a workspace with `.claude/skills` → 3 insert styles land in the composer.
**Done:** gates + live checks; commit.

### P12 — Settings + onboarding + demo adapter + responsive 3-column + deep links (cmux-harness)

**Goal:** the app is complete as a product surface: configurable, offline-usable for dev, correct at all three form factors, deep-linkable.
**Create:** `src/components/Settings/SettingsModal.tsx` (sections byte-exact: `Connection` card with `Server`/`Workspaces`/`Live panes`/`Last update`, `Server URL` field + `Save and reconnect`, `Connect a real server`; `Use demo data` toggle; `Attention` / `Smart agent alerts` (local badge semantics); `Delivery` row derived from `GET /push/status` — on this Mac `apns.configured:false` → show `Local alerts while the app is connected`; `Private by design` rows byte-exact: `The raw Herdr socket never leaves your Mac` / `Terminal control requires your pairing token` / `Tailscale keeps the server inside your tailnet`; About `Remote command deck · 0.1`), `src/components/Onboarding/OnboardingView.tsx` (byte-exact: `herdr`, `Your agents, within reach`, `Know where to look.`, `Move from workspace to pane to live agent in seconds. Herdr keeps the terminals real; this app keeps the decisions close.`, `Connect to your Mac`, URL placeholder `https://your-mac.tailnet.ts.net`, `Pairing token`, `Connect`, footnote adapted: token stays in this browser (localStorage) — the Keychain line is iOS-only, `Explore with live-looking demo data`; URL validation: `https` anywhere, `http` only for localhost/127.0.0.1/::1, else `Use HTTPS, or HTTP only when connecting to localhost.`), `src/demo/` (mock fetch adapter keyed on a flag — port `DemoData` from the worktree iOS app (locate via `rg "DemoData" cmux-herdr-harness/herdr-harness-ios`); a few workspaces + tabs + panes + alerts + one fake Pi conversation; feeds the same stores; `Demo data is active` banner + connection state `Demo`), responsive layout: **≥1100 px → 3-column** (workspaces | panes | session, the iPad parity); 700-1100 px → 2-column + drawer; <700 px → single column (Phase 1 <900 px drawer pattern, inverted to rail on desktop), deep links: `#pane=<id>` (+`#ws=`) with **pending-pane queue** (select before data loaded → queue → resolve when `/workspaces` arrives; parity with doc 01 §2), `repairNavigation` route guards wired in, `connection.issue` error alert (`Connection issue` + `Dismiss`) + toasts 2.2 s auto-dismiss.
**Runs:** 1-2 — (a) settings + onboarding + demo adapter; (b) responsive breakpoints + deep links + route repair.
**Verify:** tsc/vitest/build; **browser at 3 viewports** (CDP: 1440×900, 1024×768, 390×844) — 3 columns collapse correctly; direct-load `…/herdr-web/#pane=w1:p1` with a valid token → pane selected once data lands; demo mode: full navigation offline (kill nothing — just the mock flag); onboarding: wrong token → 401 handling → re-entry; settings: save URL + reconnect works.
**Done:** gates + viewport pass; commit.

### P13 — Polish + adversarial review + browser QA (both repos)

**Goal:** the Phase-1-style finish: adversarial review of the full diff, fixes, and a live browser QA pass proving the DoD.
**Steps (orchestrator-driven; coder runs only for the fix list):**
1. Full gates: `tsc --noEmit`, `vitest run`, `vite build` (cmux-harness); `python3 -m unittest` NOT applicable (no Python test changes — verify by diff that only `server.py` changed in the worktree); `git status --short` in worktree = the original 4 dirty lines + `?? herdr_harness/static/` only (**dirty-file integrity check**).
2. Reviewer subagent (fresh session) runs the comprehensive-pr-review skill by name on the full `feat/harness-web-app` diff (herdr-web scope); orchestrator adjudicates; coder fix runs (fresh) on the approved list only; re-review the fix diff; commit.
3. Browser QA (throwaway Chrome + CDP, Phase-1 style): screenshots of every parity surface at 9092 with live data — onboarding, sidebar+list, Attention Deck, terminal (live frames), Pi Chat (streaming pane + 501 pane), Command Lens, radar + a split action, Git tab, Skills, Settings, phone width.
4. Tailnet matrix: `curl -sk` with/without Bearer on `/herdr-web/` (200/200 — static is pre-auth), `/api/v1/health` (200/401 byte-exact), one SSE stream through the TLS proxy for 10 s (chunked-encoding sanity — doc 03 assumption 3: verify fetch streaming survives `tailscale serve`).
5. Cost ledger update (`~/.pi/agent/spend/usage.jsonl`) + Slack report (slack-me) with URL + token + QA list.
**Done:** all DoD items below checkable live; no pushes anywhere; final commit in cmux-harness (fixes) — worktree receives no further changes.

---

## Cross-cutting

**Test strategy.** vitest per module (repo convention: `*.test.ts(x)` beside source). Fixture sources, in priority order:

| Port | Fixture source (exact) |
|---|---|
| `grid.ts` | `cmux-herdr-harness/herdr-harness-ios/herdr-harness-iosTests/TerminalGridHardeningTests.swift` (port 1:1) + synthetic full/delta ANSI + **live-captured** `terminal-deltas.json` (P5) |
| `refreshPolicy` | no Swift test exists — table-driven from doc 01 §4.5 rules (forced / no-advance / text-changed-no-frames / ≥25 s silence / failed-snapshot×{fresh,stale} stream) |
| `reducer.ts` / `envelope.ts` | `herdr-harness-iosTests/PiConversationReducerTests.swift` (full table) + `PiConversationDecodingTests.swift` + `PiContextUsageTests.swift` + doc 02 §3.3 wire samples |
| stores | live JSON fixtures in `src/__fixtures__/` captured by the orchestrator (doc 02 §7 shapes: workspace composite, alert, pane+`pi_semantic`, layout, health, pi/snapshot) |
| views | fixture-driven store states, incl. all 3 Pi capability variants (doc 02 §5) and `DemoData` mock (P12) for offline paths |

The 18-file unit suite in `herdr-harness-ios/herdr-harness-iosTests/` is the behavioral spec (doc 01 header counts 20 incl. the 2 UI-test files, which are out of scope — no UI-test equivalent planned).

**Error surfaces (both-servers dependency, doc 03 risk 5).** git/skills/files/Jira/attachments proxy to 9091 (`HERDR_HARNESS_CMUX_URL` default `127.0.0.1:9091`); 9091 down → those tools 502 while the app otherwise looks live. Every proxied tool renders `ToolErrorCard` (`<Tool> unavailable` byte-exact + `Try again`/`Retry`); connection pill uses `/health` (`herdr.connected` vs `cache.stale`); onboarding fine print notes the dependency. 401 anywhere → onboarding re-entry. Terminal 503 `terminal_observer_unavailable` → source `offline` + server message (expected only pre-P1).

**Security checklist (re-verify at P13).** (1) Token in `#fragment` + localStorage only; `Authorization: Bearer` header on every request; **token never in any URL query string, log, or proxy record** (the concrete win of fetch-SSE, doc 03 Decision 1) — orchestrator greps the diff for `?token` in request construction → zero. (2) **No `dangerouslySetInnerHTML` anywhere**; Pi/bridge markdown via constrained allowlist renderer only. (3) CORS left OFF (env unset) — same-origin after P1; `do_OPTIONS` stays dormant. (4) Static mount has the traversal guard (P1 check 3). (5) Pre-auth static serves only `index.html` + assets — no secrets in the shell (the `SETUP_HTML` precedent). (6) 1 MB body caps server-side already; attachment uploads ≤20 MB client-enforced. (7) No loopback bypass added (deliberately — doc 03 security).

**A11y / responsive (doc 00 §7 parity).** 3-column desktop (workspaces | panes | session) ≥1100 px, 2-col + drawer mid, 1-col phone; key deck are real `<button>`s (focusable, `aria-label` per key); source labels are text + dot, never color-only; toasts `role="status"`; menus/confirm modals trap focus + Esc (Phase 1 `useOverlay` port); terminal `<pre>` selectable; radar has an accessible list fallback (pane list already renders the same data); byte-exact strings are also the a11y contract — screen readers announce the same words as iOS.

---

## Risks & mitigations in execution order (doc 03's 9, mapped)

| # | Risk (doc 03) | Handled in | Mitigation (as planned) |
|---|---|---|---|
| P1 | `replay_gap` is the **normal** path (1024 ring vs >100 ev/s) | **P2** | `stream.reset` → silent refetch, never an error banner; synthetic gap fixture in `eventStream.test.ts`; iOS backoff values |
| P2 | Pi replay cost (~2 MB fresh connect) | **P2 + P7** | one-time `?after=0` replay on first connect only; reconnect sends last-seen cursor (`Last-Event-ID` / `?after=`); `pi.*` journal re-publications ignored in global state; 500 ms debounce |
| P3 | Terminal delta frames never captured on the wire | **P5** | live capture step (send-keys + observe → 2-3 delta fixtures + regression test); `seq` dedup + 850 ms snapshot arbitration bound corruption to one frame cycle; snapshot-only = accepted degraded mode (iOS parity) |
| P4 | Terminal stream 503 blocks all terminal QA | **P1** | launch-script `HERDR_BIN_PATH` line + flagged kickstart milestone; verified by the `/stream` 200 gate before P5 starts |
| P5 | Both-servers dependency (silent 502s) | **P11** (+P12 onboarding) | per-tool error cards with byte-exact strings; `/health` `herdr.connected`/`cache.stale` split; dependency documented; 9091 never stopped for QA — 502 path unit-tested |
| P6 | Uncommitted worktree shim ≠ running server | **all phases** | running 9092 is the QA source of truth; the 4 dirty files are on the global do-not-touch list; flagged for Ronnie to commit (his files, not ours) |
| P7 | Pi capability skew + `interactionResponse` 501 everywhere | **P6 + P8** | per-pane `capabilities` from snapshot (never global); chips hidden when unsupported; respond → 501 → graceful notice; fixture tests for all 3 live variants |
| P8 | Dirty alert journal (123/125 unread) | **P3** | deck assumes unread state; `unreadCount`/read-all only; never clear-on-load |
| P9 | Stdlib-server fragility on additions | **P1** | server diff frozen at exactly 2 changes (mount ~30-40 LOC modeled on a proven pattern + 1 env line); no new endpoints, no new Python deps |

---

## Out of scope (v1)

- **Voice** (decision #6, ⚖️ sign-off pending) — server strictly 16 kHz WAV; `voice` chip disabled only.
- **Web Push / APNs replacement** — no VAPID endpoints exist (decision #3).
- **HerdPulse UI** (Live Activities/lock screen) — no browser equivalent; no GET endpoint; Web-Notifications pulse is a documented follow-up (decision #2).
- **Haptics** — no meaningful Mac-web equivalent (decision #7).
- **Dead code** — `FleetSummaryView`, `PaneSessionHeader` (0 references) not ported (decision #7).
- **The uncommitted worktree shim** (Ronnie's `client.py`/`service.py`/`pbxproj`/`xcscheme`) — never touched, never committed, no-op at protocol 19 today.
- `herdr://` URL scheme handling (hash routes replace it); xterm.js / unbounded scrollback (fixed 100×32 grid, snapshot fallback); `GET /api/v1/live-activities` (doesn't exist); legacy `/api/*` routes (don't exist on 9092 — 404 verified).

---

## Definition of done (whole project)

All checkable **live** at the current tailnet 8461 URL (doc-verified: `https://ronniesitym4mbp.tail1db61d.ts.net:8461/herdr-web/` — orchestrator re-confirms hostname via `tailscale serve status` at P13; the planning brief's quoted hostname differs and is treated as stale) with the Bearer token, plus the 9092 loopback URL:

1. **Onboarding & auth:** token entry (modal + `#token=`) → live app; wrong token → 401 → re-entry; byte-exact onboarding strings; token never in a URL.
2. **Attention Deck:** `Attention deck` with ranked `Live queue` (blocked→done→working→idle→unknown) + `Recent signals` journal, `NEW` capsule, unread badge, mark-read + read-all; byte-exact strings; survives the 123-unread dirty journal.
3. **Workspace list + 3-column layout:** search, `All`/`Needs you`/`Active`, cards with status + `rev N`, AttentionStrip top-2, `spaces` count; 3-column desktop / drawer mid / 1-col phone; byte-exact strings; deep link `#pane=<id>` with pending-pane queue.
4. **Terminal:** live grid frames (source `live`, `W×H · fN`), keypress updates within a frame cycle, 850 ms snapshot arbitration, `connecting/live/watching/offline` labels, reconnect on tab resume; delta fixtures in repo from live capture.
5. **Command Lens:** status-aware placeholders, `run` vs `prompt` dispatch, 8-key deck, `attach`/`@ file`/`jira` inserts (mic disabled), attachments `attached`; byte-exact strings + toasts.
6. **Pi Chat (full):** banner states, context meter, streaming turns, thinking disclosure, tool cards (QUEUED/RUNNING/DONE/FAILED), interaction cards with 501 notice, status bar + `Stop`, model/thinking chips per-pane capability-gated across all 3 live variants; byte-exact strings.
7. **Tools & mutations:** Topology Radar matching the live layout; split/focus/close/rename/start-agent via menus with confirm dialogs and post-action refresh; Git/Skills/Files/Jira tabs against `/api/v1/workspaces/{id}/…` with per-tool 502 error cards.

Plus: Phase-1-style browser QA pass (P13 step 3, screenshots archived); tailnet curl matrix green (P13 step 4); worktree `git status --short` = original 4 dirty lines (+ untracked `static/`); no pushes in either repo; cost ledger + Slack report delivered (P13 step 5).
